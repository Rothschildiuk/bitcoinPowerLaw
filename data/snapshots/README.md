Checked-in prepared daily snapshots live here.

These files are meant to be refreshed by `scripts/update_data_snapshots.py`.
The Streamlit app prefers these local snapshots at runtime to avoid fetching
external data on every page load.
